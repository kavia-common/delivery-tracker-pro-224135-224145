# Example environment variables for database tooling
# NOTE: Do not commit actual secrets. Copy to .env and set real values.

# PostgreSQL connection URL for Alembic and tooling
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/delivery_tracker

# Frontend-related variables (present in project-level .env) are not needed for DB tooling,
# but are listed in the request details. Keep DB-specific variables here only.
